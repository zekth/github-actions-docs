name: 'PR'
on:
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        id: doc
      - run: echo ${{steps.doc.outputs.doc}}
